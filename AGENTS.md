# Agent Instructions

- Run `flake8` and `pytest` before committing changes.
- Follow the existing style conventions described in `STYLEGUIDE.md`.
- Commit directly to the default branch; do not create new branches.
